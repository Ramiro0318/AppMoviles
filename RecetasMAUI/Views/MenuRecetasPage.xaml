<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="RecetasMAUI.Views.MenuRecetasPage"
             xmlns:vm="clr-namespace:RecetasMAUI.ViewModels"
             Title="MenuRecetasPage">
    <VerticalStackLayout>
        <SearchBar></SearchBar>
        <VerticalStackLayout>
            <Border Stroke="DarkGray" StrokeThickness="1" Background="White">
                <Grid RowDefinitions="*,*" ColumnDefinitions="0.2*,0.4*,0.4*">
                    <Image Grid.Column="0" Grid.RowSpan="2"></Image>
                    <Label Grid.Column="1" FontAttributes="Bold">budin de pan y manxanas</Label>
                    <Label Grid.Column="1">105 minutos</Label>
                    <Label Grid.Column="2">10 porciones</Label>
                </Grid>
            </Border>
        </VerticalStackLayout>
    </VerticalStackLayout>
</ContentPage>